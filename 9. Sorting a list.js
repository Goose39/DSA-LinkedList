sortList = (list) => {
  // Only one item in list
  if (list.head === null) {
    return list.head
  }

  let prevNode = list.head;
  let currNode = list.head;

  while ( currNode != prevNode && currNode != null ) {
    if (currNode < prevNode) {

    }
  }

}